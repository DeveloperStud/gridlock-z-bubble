# gridlock-z

See full docs in the origin repo path `docs/GRIDLOCK-Z.md` / braid-shell.

## Cloud bubble
- GitHub mirror: this repo
- trycloudflare = ephemeral (dies with box)
- Restore: copy site/ → www/seed → braid-site run → free ensure → failsafe sync → bubble sync

## CLI (on origin IIS)
POST /api/cli allowlisted; shell needs unlock+confirm off public.
